// This file is no longer used for providing reports to the dashboard,
// as data is now fetched directly from Firestore.
// It can be kept for reference or future use if needed.

import type { Report } from './types';

export const mockReports: Report[] = [];
